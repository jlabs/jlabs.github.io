import React from 'react'

import Layout from "../components/layout"

const About = ({location}) => (
    <Layout location={location}>
        <div>
            <h1>About</h1>
            <p>This page is about a page</p>
        </div>
    </Layout>
)

export default About